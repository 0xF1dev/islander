[package]
name = "Islander"
version = "0.1.0"
edition = "2024"

[dependencies]
ansi_term = "0.12.1"
clap = { version = "4.6.6", features = ["derive"] }
image = { version = "0.25.10", default-features = false, features = ["png"] }
rand = "0.10.2"
spinners = "4.2.0"

[profile.release]
codegen-units = 1
lto = "thin"
opt-level = "z"
